[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prepared-knn-search"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cuda-python",
    "torch",
]

[project.optional-dependencies]
test = [
    "pytest",
]
benchmark = [
    "cupti-python",
    "cuda-pathfinder",
    "nvidia-cuda-cupti",
    "pytest",
    "triton",
]
tvm-ffi = [
    "apache-tvm-ffi>=0.1.12,<0.2",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"flashlib_cake_knn_search" = ["manifest.json", "cuda/*.cu"]

