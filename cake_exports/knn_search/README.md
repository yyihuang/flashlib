# Exported Loom Kernels

This repository was generated by `loom.export.kernel_repo`. It contains frozen
CUDA source plus a lightweight Python binding that compiles the source with
NVRTC and launches it through CUDA driver APIs.

## Repository Layout

```text
src/flashlib_cake_knn_search/
  __init__.py          # public Python exports
  kernels.py           # KernelSpec, get_kernel(), launch_<kernel>() wrappers
  _runtime.py          # NVRTC + CUDA driver launch support
  _benchmark.py        # strict CUPTI timing with cold-L2 flushing
  manifest.json        # provenance, parameter order, launch metadata
  cuda/*.cu            # frozen bundled CUDA sources; no Weave IR
tests/
  test_exported_kernels.py
  test_benchmark_harness.py
benchmarks/
  benchmark_exported_kernels.py
  benchmark_shapes.py  # semantic correctness + CUPTI performance runner
  workload.py          # workload shapes/reference/metric adapter
RESULTS.md             # correctness and performance results
```

## Kernels

- `search_knn_search_warp_direct_v1`: `kernel_knn_search_warp_direct_v1`, launch_mode=`normal`, threads=256, shared_mem=640, params=(queries, database, out_distances, out_indices, B, Q, M, K)
- `search_knn_search_warp_split_partial_v1`: `kernel_knn_search_warp_split_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(queries, database, partial_distances, partial_indices, B, Q, M, K, num_m_tiles)
- `search_knn_search_warp_split_merge_v1`: `kernel_knn_search_warp_split_merge_v1`, launch_mode=`normal`, threads=256, shared_mem=20480, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, num_m_tiles)
- `search_q1_knn_search_q1_tile_reduce_partial_v1`: `kernel_knn_search_q1_tile_reduce_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=5120, params=(queries, database, partial_distances, partial_indices, B, Q, M, K, num_m_tiles)
- `search_q1_knn_search_q1_tile_reduce_merge_v1`: `kernel_knn_search_q1_tile_reduce_merge_v1`, launch_mode=`normal`, threads=256, shared_mem=640, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, num_m_tiles, num_groups, tiles_per_group)
- `knn_search_base_ir`: `kernel_knn_search_lowq_row16_mma_partial_dispatch0610_r18_d14a_vec16_bguard_v1`, launch_mode=`normal`, threads=512, shared_mem=96000, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles)
- `knn_search_b2_q128_ir`: `kernel_knn_search_mma_split_partial_v1`, launch_mode=`normal`, threads=640, shared_mem=108800, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_blockm640_ir`: `kernel_knn_search_lowq_tile_reduce_partial_0614_r10_e864_blockm640_v1`, launch_mode=`normal`, threads=256, shared_mem=5120, params=(queries, database, partial_distances, partial_indices, B, Q, M, K, num_m_tiles)
- `knn_search_blockm640_merge_ir`: `kernel_knn_search_lowq_tile_reduce_merge_0614_r10_e864_blockm640_v1`, launch_mode=`normal`, threads=256, shared_mem=640, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, num_m_tiles, num_groups, tiles_per_group)
- `knn_search_b2_q128_merge_ir`: `kernel_knn_search_mma_split_merge_stream_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, split_m, num_q_tiles)
- `knn_search_blockm896_ir`: `kernel_knn_search_lowq_tile_reduce_partial_0614_r11_e864_blockm896_v1`, launch_mode=`normal`, threads=256, shared_mem=5120, params=(queries, database, partial_distances, partial_indices, B, Q, M, K, num_m_tiles)
- `knn_search_blockm896_merge_ir`: `kernel_knn_search_lowq_tile_reduce_merge_0614_r11_e864_blockm896_v1`, launch_mode=`normal`, threads=256, shared_mem=640, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, num_m_tiles, num_groups, tiles_per_group)
- `knn_search_q3_partial_ir`: `kernel_knn_search_lowq_tile_reduce_partial_m131072_exact_0617_cc76_v1`, launch_mode=`normal`, threads=256, shared_mem=5120, params=(queries, database, partial_distances, partial_indices, Q)
- `knn_search_q3_merge_ir`: `kernel_knn_search_lowq_tile_reduce_merge_m131072_exact_0617_cc76_v1`, launch_mode=`normal`, threads=128, shared_mem=384, params=(partial_distances, partial_indices, out_distances, out_indices)
- `knn_search_scalar_capacity_ir`: `kernel_knn_search_scalar_capacity_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(queries, database, partial_distances, partial_indices, B, Q, M, num_m_tiles)
- `knn_search_k1_merge8_partial_ir`: `kernel_knn_search_q4096_lowk_k1partial_onestage_0614_r2_3ff5_v1`, launch_mode=`normal`, threads=640, shared_mem=108800, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_k1_merge8_merge_ir`: `kernel_knn_search_k1_top1_margin_0614_r93_merge8_v1`, launch_mode=`normal`, threads=128, shared_mem=0, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, split_m, num_q_tiles)
- `knn_search_q1_m262144_ir`: `kernel_knn_search_q1_irregular_m_tail_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=5120, params=(queries, database, partial_distances, partial_indices, B, Q, M, K, num_m_tiles)
- `knn_search_q1_m262144_merge_ir`: `kernel_knn_search_q1_flashdecode_merge128_0614_r92_v1`, launch_mode=`normal`, threads=256, shared_mem=640, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, num_m_tiles, num_groups, tiles_per_group)
- `knn_search_b2_k64_partial_ir`: `kernel_knn_search_80a5_b2_q128m65536_k64_twotile_partial_74f4_v1`, launch_mode=`normal`, threads=512, shared_mem=165120, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_b2_k64_group_merge_ir`: `kernel_knn_search_k64_q128m65536_groupmerge64_kexact_0614_r27_k64thin_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(partial_distances, partial_indices, group_distances, group_indices, B, Q, K, num_q_tiles)
- `knn_search_b2_k64_final_merge_ir`: `kernel_knn_search_k64_q128m65536_finalmerge16_kexact_0614_r27_k64thin_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(group_distances, group_indices, out_distances, out_indices, B, Q, K)
- `knn_search_d384_q256_partial_ir`: `kernel_knn_search_d384_mma_split_partial_0612_r34_v1`, launch_mode=`normal`, threads=640, shared_mem=126720, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_d384_q256_merge_ir`: `kernel_knn_search_mma_split_merge_q128_const148_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, split_m, num_q_tiles)
- `knn_search_k64_q256_partial_ir`: `kernel_knn_search_80a5_blocker_k64_q256_m65536_twotile_partial_v1`, launch_mode=`normal`, threads=512, shared_mem=165120, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_lowd_d256_ir`: `kernel_knn_search_mma_split_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=143104, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_lowd_d256_merge_ir`: `kernel_knn_search_mma_split_merge_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, split_m, num_q_tiles)
- `knn_search_lowd_d256_k64_partial_ir`: `kernel_knn_search_mma_split_partial_v1`, launch_mode=`normal`, threads=256, shared_mem=143104, params=(queries, database, partial_distances, partial_indices, B, Q, M, split_m, num_q_tiles, total_m_tiles, tiles_per_split)
- `knn_search_lowd_d256_k64_merge_ir`: `kernel_knn_search_mma_split_merge_v1`, launch_mode=`normal`, threads=32, shared_mem=0, params=(partial_distances, partial_indices, out_distances, out_indices, B, Q, K, split_m, num_q_tiles)
- `knn_search_lowd_dbscan_ir`: `kernel_knn_search_lowd_dbscan_d2_t128_m1536_0613_r56_cd72_v1`, launch_mode=`normal`, threads=128, shared_mem=12416, params=(queries, database, out_distances, out_indices, B, Q, M, K)

## Install

```bash
python -m pip install -e .
```

The runtime expects PyTorch and a CUDA Python package that provides
`cuda.bindings.driver` and `cuda.bindings.nvrtc`.

## API Entry Points

The generated API below is a low-level binding. For a target-specific library
export, add a semantic API module that matches the workload contract and hides
the internal kernel decomposition. For example, a KNN export should expose
separate `knn_build(database, k, ...)` and
`knn_search(query, database, k, ...)` entry points, with tests and benchmarks
that exercise those semantic entry points.

- `flashlib_cake_knn_search.KERNELS`: mapping from exported kernel name to `ExportedKernel`.
- `flashlib_cake_knn_search.get_kernel(name)`: inspect metadata, source text, or compile.
- `flashlib_cake_knn_search.launch_<kernel>(*args, grid=...)`: low-level launch wrapper.
- `ExportedKernel.source_text()`: read the frozen CUDA source.
- `ExportedKernel.compile(arch="sm_100a")`: compile with NVRTC.
- `ExportedKernel.launch(...)`: launch with explicit CUDA parameter order.

```python
import torch
from flashlib_cake_knn_search import get_kernel, launch_search_knn_search_warp_direct_v1

kernel = get_kernel("search_knn_search_warp_direct_v1")
print(kernel.parameters)

# Pass CUDA tensors or scalar values in the parameter order shown in
# src/flashlib_cake_knn_search/manifest.json. Pointer parameters accept CUDA tensors
# or integer device pointers.
launch_search_knn_search_warp_direct_v1(*args, grid=(1, 1, 1))
torch.cuda.synchronize()
```

## Correctness And Performance

Recorded correctness and performance results live in `RESULTS.md`.
`benchmarks/workload.py` is the workload adapter: it declares concrete shapes
and builds candidate/reference/compare callables for each shape. When the
export command receives `--benchmark-adapter path/to/workload.py`, the
generated repository is immediately runnable; otherwise the file is an
explicit template that must be completed before performance can be reported.

## Tests

The exported repository includes unit tests that do not require a GPU or CUDA
Python bindings:

```bash
python -m pip install -e ".[test]"
pytest
```

## Benchmarks

The generated benchmark script measures NVRTC compile latency for the frozen
CUDA sources. It is a build/runtime smoke benchmark, not a semantic kernel
throughput benchmark:

```bash
python benchmarks/benchmark_exported_kernels.py --arch sm_100a --json results/compile_benchmark.json
python benchmarks/benchmark_shapes.py --json results/shape_benchmark.json
```

The shape runner checks correctness before timing and refuses to report
performance unless the requested timing backend is CUPTI. Each result records
the shape, tolerance/comparison details, median kernel time, and optional
TFLOPS/GB/s supplied by the workload adapter.
