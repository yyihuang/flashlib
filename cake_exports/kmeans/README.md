# Exported Loom Kernels

This repository was generated by `loom.export.kernel_repo`. It contains frozen
CUDA source plus a lightweight Python binding that compiles the source with
NVRTC and launches it through CUDA driver APIs.

## Complete Workload Entry Points

This export is a complete workload package. Its three review and execution
entry points are deliberately separate and have stable names:

- Python interface: [`src/flashlib_cake_kmeans/interface.py`](src/flashlib_cake_kmeans/interface.py) — public APIs: `flash_kmeans_assign`
- GPU correctness test: [`tests/test_correctness.py`](tests/test_correctness.py)
- CUPTI performance benchmark: [`benchmarks/benchmark.py`](benchmarks/benchmark.py)

```bash
pytest tests/test_correctness.py -q
python benchmarks/benchmark.py --no-correctness --json results/performance.json
```

The public interface can be imported directly from the package root; the
implementation contains frozen CUDA and bindings but no Weave IR.

## Repository Layout

```text
src/flashlib_cake_kmeans/
  __init__.py          # public Python exports
  kernels.py           # KernelSpec, get_kernel(), launch_<kernel>() wrappers
  _runtime.py          # NVRTC + CUDA driver launch support
  _benchmark.py        # strict CUPTI timing with cold-L2 flushing
  manifest.json        # provenance, parameter order, launch metadata
  cuda/*.cu            # frozen bundled CUDA sources; no Weave IR
tests/
  test_exported_kernels.py
  test_benchmark_harness.py
benchmarks/
  benchmark_exported_kernels.py
  benchmark_shapes.py  # semantic correctness + CUPTI performance runner
  workload.py          # workload shapes/reference/metric adapter
RESULTS.md             # correctness and performance results
```

## Kernels

- `kmeans_v10`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_v10`, launch_mode=`normal`, threads=192, shared_mem=100352, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_v15`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_v15`, launch_mode=`normal`, threads=192, shared_mem=133120, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_d64_direct`: `kernel_flash_kmeans_assign_d64_direct_1p2gap_9f2a_v1`, launch_mode=`normal`, threads=192, shared_mem=51200, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_microdim_direct`: `kernel_flash_kmeans_assign_microdim_direct_9c0d_v1`, launch_mode=`normal`, threads=192, shared_mem=51200, params=(x, centroids, x_sq, c_sq, out, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_microdim_pack`: `kernel_flash_kmeans_assign_microdim_pack_6cd2_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(x, centroids, x_pad, c_pad, B, N, D, K, total_x_pad, total_c_pad)
- `kmeans_microdim_main`: `kernel_flash_kmeans_assign_microdim_6cd2_v1`, launch_mode=`normal`, threads=192, shared_mem=51200, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_lowdim_pack`: `kernel_flash_kmeans_assign_lowdim_pack_e50c_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(x, centroids, x_pad, c_pad, B, N, D, K, total_x_pad, total_c_pad)
- `kmeans_lowdim_main`: `kernel_flash_kmeans_assign_lowdim_e50c_v1`, launch_mode=`normal`, threads=192, shared_mem=100352, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_gap_pad_pack`: `kernel_flash_kmeans_assign_gap_pad_pack_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(x, centroids, x_pad, c_pad, B, N, D, K, D_PAD, total_x_pad, total_c_pad)
- `kmeans_pad192_pack`: `kernel_flash_kmeans_assign_d160_pad192_pack_f9b2_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(x, centroids, x_pad, c_pad, B, N, D, K, total_x_pad, total_c_pad)
- `kmeans_d160_padded_pack`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d160_pack_padded_b23d_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(x, centroids, x_pad, c_pad, B, N, D, K, total_x_pad, total_c_pad)
- `kmeans_d160_splitd`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d160_splitd_v1`, launch_mode=`normal`, threads=192, shared_mem=165888, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_d192_single`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d192_single_v1`, launch_mode=`normal`, threads=192, shared_mem=149504, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_d192_splitd`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d192_splitd_v1`, launch_mode=`normal`, threads=192, shared_mem=198656, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_d256_single`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d256_single_v1`, launch_mode=`normal`, threads=192, shared_mem=198656, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_d256_splitd`: `kernel_flash_kmeans_assign_cleanroom_tcgen05_d256_splitd_v1`, launch_mode=`normal`, threads=192, shared_mem=264192, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_highd_splitd`: `kernel_flash_kmeans_assign_highd_splitd_6fcf_v1`, launch_mode=`normal`, threads=192, shared_mem=51200, params=(x_sq, c_sq, out, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_highd_splitk_partial`: `kernel_flash_kmeans_assign_highd_splitk_partial_8de8_v1`, launch_mode=`normal`, threads=192, shared_mem=51200, params=(c_sq, partial_scores, partial_indices, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles)
- `kmeans_highd_splitk_reduce`: `kernel_flash_kmeans_assign_highd_splitk_reduce_8de8_v1`, launch_mode=`normal`, threads=128, shared_mem=0, params=(partial_scores, partial_indices, out, B, N, K, num_n_tiles, K_tiles)
- `kmeans_highd_splitk_blockn64_g2r4_partial`: `kernel_flash_kmeans_assign_highd_splitk_partial_blockn64_g2r4_b5a6_v1`, launch_mode=`normal`, threads=192, shared_mem=43008, params=(c_sq, partial_scores, partial_indices, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles, K_slices)
- `kmeans_highd_splitk_blockn64_g2r4_reduce`: `kernel_flash_kmeans_assign_highd_splitk_reduce_blockn64_g2r4_b5a6_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(partial_scores, partial_indices, out, B, N, K, num_n_tiles, K_slices)
- `kmeans_highd_splitk_blockn64_g1r4_partial`: `kernel_flash_kmeans_assign_highd_splitk_partial_blockn64_g1r4_streamdep_r63_v1`, launch_mode=`normal`, threads=192, shared_mem=43008, params=(c_sq, partial_scores, partial_indices, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles, K_slices)
- `kmeans_highd_splitk_blockn64_g1r4_reduce`: `kernel_flash_kmeans_assign_highd_splitk_reduce_blockn64_g1r4_streamdep_r63_v1`, launch_mode=`normal`, threads=256, shared_mem=0, params=(partial_scores, partial_indices, out, B, N, K, num_n_tiles, K_slices)
- `kmeans_highd_paired_xreuse_r47_partial`: `kernel_flash_kmeans_assign_highd_paired_xreuse_dualtmem_producer_r47_v1`, launch_mode=`normal`, threads=192, shared_mem=75776, params=(c_sq, partial_keys, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles, K_slices)
- `kmeans_highd_paired_ownerreduce_r39_reduce1`: `kernel_flash_kmeans_assign_highd_paired_ownerreduce_r39_reduce1_unroll_v1`, launch_mode=`normal`, threads=64, shared_mem=0, params=(partial_keys, out, B, N, K, num_n_tiles, K_slices)
- `kmeans_highd_paired_packedpartial_r2_partial`: `kernel_flash_kmeans_assign_highd_paired_packedpartial_producer_7b3c_v1`, launch_mode=`normal`, threads=192, shared_mem=43008, params=(c_sq, partial_keys, x_tmap, c_tmap, B, N, D, K, num_n_tiles, K_tiles, K_slices)
- `kmeans_highd_paired_packedpartial_r2_reduce`: `kernel_flash_kmeans_assign_highd_paired_packedpartial_reduce_r2_7b3c_v1`, launch_mode=`normal`, threads=128, shared_mem=0, params=(partial_keys, out, B, N, K, num_n_tiles, K_slices)

## Install

```bash
python -m pip install -e .
```

The runtime expects PyTorch and a CUDA Python package that provides
`cuda.bindings.driver` and `cuda.bindings.nvrtc`.

## API Entry Points

The complete workload API is exported from the package root. The bindings below
remain available for inspecting or launching individual frozen kernels.

- `flashlib_cake_kmeans.KERNELS`: mapping from exported kernel name to `ExportedKernel`.
- `flashlib_cake_kmeans.get_kernel(name)`: inspect metadata, source text, or compile.
- `flashlib_cake_kmeans.launch_<kernel>(*args, grid=...)`: low-level launch wrapper.
- `ExportedKernel.source_text()`: read the frozen CUDA source.
- `ExportedKernel.compile(arch="sm_100a")`: compile with NVRTC.
- `ExportedKernel.launch(...)`: launch with explicit CUDA parameter order.

```python
import torch
from flashlib_cake_kmeans import get_kernel, launch_kmeans_v10

kernel = get_kernel("kmeans_v10")
print(kernel.parameters)

# Pass CUDA tensors or scalar values in the parameter order shown in
# src/flashlib_cake_kmeans/manifest.json. Pointer parameters accept CUDA tensors
# or integer device pointers.
launch_kmeans_v10(*args, grid=(1, 1, 1))
torch.cuda.synchronize()
```

## Correctness And Performance

Recorded correctness and performance results live in `RESULTS.md`.
`benchmarks/workload.py` is the workload adapter: it declares concrete shapes
and builds candidate/reference/compare callables for each shape. When the
export command receives `--benchmark-adapter path/to/workload.py`, the
generated repository is immediately runnable; otherwise the file is an
explicit template that must be completed before performance can be reported.

## Tests

The exported repository includes unit tests that do not require a GPU or CUDA
Python bindings:

```bash
python -m pip install -e ".[test]"
pytest
```

## Benchmarks

The generated benchmark script measures NVRTC compile latency for the frozen
CUDA sources. It is a build/runtime smoke benchmark, not a semantic kernel
throughput benchmark:

```bash
python benchmarks/benchmark_exported_kernels.py --arch sm_100a --json results/compile_benchmark.json
python benchmarks/benchmark_shapes.py --json results/shape_benchmark.json
```

The shape runner checks correctness before timing and refuses to report
performance unless the requested timing backend is CUPTI. Each result records
the shape, tolerance/comparison details, median kernel time, and optional
TFLOPS/GB/s supplied by the workload adapter.
